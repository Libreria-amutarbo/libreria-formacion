import { DcxPaginator } from '../interfaces';

export const defaultPaginator: DcxPaginator = {
  totalItems: 100,
  itemsPerPage: 10,
  currentPage: 2,
};

export const selectPerPage: DcxPaginator = {
  totalItems: 21,
  itemsPerPage: 5,
  currentPage: 1,
};

export const limitedPaginator: DcxPaginator = {
  totalItems: 100,
  itemsPerPage: 10,
  currentPage: 2,
};

export const knowPageSelected: DcxPaginator = {
  totalItems: 100,
  itemsPerPage: 10,
  currentPage: 2,
};
