# dcx-web-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dcx-web-lib` to build the library.

## Running unit tests

Run `nx test dcx-web-lib` to execute the unit tests via [Jest](https://jestjs.io).
